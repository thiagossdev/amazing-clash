# Godot 4+ specific ignores
.godot/
/android/

# Auto-regenerated by the editor-layout-load step of `godot4 --import`
# (and any non-headless-test launch) -- this project has no real export
# targets, so these are always empty scaffolding, never real content.
export_presets.cfg
/build/

# added by agent-md installer
.agent/
